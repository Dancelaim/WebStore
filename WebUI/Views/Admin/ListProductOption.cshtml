@model IEnumerable<WowCarry.Domain.Entities.ProductOption>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>AdminOptions</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create",new { type = "ProductOption" }, new { @class = "btn" })
    </p>
    <table class="table">
        <thead>
            <tr class="title">
                <th>Название</th>
                <th class="text-center">Действия</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>

                    <td> @Html.ActionLink(item.OptionName, "Edit", new { Id = item.ProductOptionId, type = item.GetType().BaseType.Name })</td>
                    <td class="text-center">
                        @using (Html.BeginForm("Delete", "Admin"))
                        {
                            @Html.Hidden("GameId", item.ProductOptionId)
                            <input type="submit"
                                   class="btn btn-default btn-xs"
                                   value="Удалить" />
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
</body>
</html>
